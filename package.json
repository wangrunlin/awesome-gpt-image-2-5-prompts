{
  "name": "awesome-gpt-image-2-5-prompts",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Source-backed GPT Image 2.5 prompt collection maintained by Image 2.5",
  "license": "MIT",
  "engines": { "node": ">=22" },
  "scripts": {
    "build": "node scripts/build.mjs",
    "validate": "node scripts/validate.mjs",
    "test": "node --test",
    "check": "npm run validate && npm test && node scripts/build.mjs --check",
    "metrics:snapshot": "node scripts/traffic-snapshot.mjs"
  }
}
